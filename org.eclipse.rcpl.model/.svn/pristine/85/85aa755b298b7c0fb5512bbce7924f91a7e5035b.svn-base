package org.eclipse.rcpl.client.migration;

/**
 * @author Ramin
 * 
 */
public class Migration {

	public static void main(String[] args) {
		//
		// OldDTSession sourceSession = new OldDTSession(
		// old.donationtoday.Donationtoday_1_0_0Package.eINSTANCE,
		// JOSession.SOURCE_PORT);
		// JOSession destinationSession = new JOSession(
		// Donationtoday_1_0_1Package.eINSTANCE,
		// JOSession.DESTINATION_PORT);
		//
		// // sourceSession.saveTemp();
		//
		// org.eclipse.rcpl.model.cdo.client.JOFactory factory =
		// destinationSession.getFactory();
		// Iterator<EObject> it = sourceSession.getContents().listIterator();
		// while (it.hasNext()) {
		// EObject eObject = it.next();
		// System. out.println("Class = " + eObject.getClass().getName());
		//
		// if (eObject instanceof old.donationtoday.Logins) {
		// old.donationtoday.Logins oldLogins = (old.donationtoday.Logins)
		// eObject;
		// EList<old.donationtoday.Login> oldLoginList = oldLogins
		// .getLogins();
		// for (old.donationtoday.Login oldLogin : oldLoginList) {
		// old.donationtoday.Org oldOrg = oldLogin.getOrg();
		// Org org = factory.createOrg(oldOrg.getName());
		// // org.setAddress(value);
		// org.setCharity(oldOrg.isCharity());
		// org.setCurrency(destinationSession.findCurrency(oldOrg
		// .getCurrency().getCode()));
		// org.setExternal(oldOrg.isExternal());
		// org.setExternalUrl(oldOrg.getExternalUrl());
		// org.setHeaderUrl(oldOrg.getOrganisationHeaderURL());
		// org.setSite(oldOrg.getSite());
		// org.setTaxNumber(oldOrg.getTaxNumber());
		// org.setType(OrgType.get(oldOrg.getType().getValue()));
		// Login login = destinationSession.getFactory().createLogin(
		// org, null, oldLogin.getUser(),
		// oldLogin.getPassword(), oldLogin.getLanguage());
		// login.setCreationDate(oldLogin.getCreationDate());
		// login.setLastLogin(oldLogin.getLastLogin());
		// login.setType(LoginType.get(oldLogin.getType().getValue()));
		// }
		// }
		//
		// // Logins
		// // Orgs
		// // ModelVersion
		// // Persons
		// // CoreData
		// // Docs
		// // Donors
		// // Projects
		//
		// }

	}

}
